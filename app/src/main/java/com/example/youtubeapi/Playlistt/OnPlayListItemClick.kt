package com.example.youtubeapi.ui.fragments.detail

import com.example.youtubeapi.Playlistt.Items

interface OnPlaylistClick { fun onPlaylist(videoId: Items) }

interface PlayList{fun OnPlayList(item : Items)}